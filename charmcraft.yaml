# Learn more about charmcraft.yaml configuration at:
# https://juju.is/docs/sdk/charmcraft-config
type: "charm"
platforms:
  ubuntu@24.04:amd64:
parts:
  charm:
    plugin: uv
    uv-extras: [charm]
    source: .
    build-packages: [pkg-config, libffi-dev, libssl-dev, rustup, build-essential python3-dev]
    build-snaps: [astral-uv]
